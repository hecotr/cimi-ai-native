# CimiLoop V1 验收报告

> 日期：2026-09-20
>
> 分支：`codex/v1-complete`
>
> 基线：Node.js 24.15.0 + pnpm 12.4.2
>
> 基础提交：`2ff20b2833054db5117622201b882cbcd85411c5`（冻结 M0 / origin/main）

## 1. 门禁结果

| 门禁 | 结果 |
|---|---|
| `pnpm schema:export` + `git diff --exit-code -- packages/protocol/schemas` | PASS（仅 CRLF 提示，无内容 diff） |
| 锁定 `pnpm test` | PASS：72 files / 264 tests |
| `git diff --check` | PASS |
| demo-m1.ps1 | PASS：CHG-0001 Planned，8 events |
| demo-m2.ps1 | PASS：Run completed + Artifact |
| demo-m3.ps1 | PASS：Independent Evaluation ALLOW |
| demo-m4.ps1 | PASS：test release verified，同一 Artifact digest |
| demo-v1.ps1 | PASS：Feature closed + exported |
| audit-v1.ps1 | PASS：无 secret 扫描命中；fault/security 8 tests |
| 隔离 Export/Import | PASS：`scripts/freeze-export-import.mjs` |
| Store 升级 | PASS：`packages/store-sqlite/test/m1-migration.test.ts`–`m5-migration.test.ts` |

demo-v1 实测：Evaluation ALLOW，Production release verified，Export digest `bb87ce816cc012ca4f9efca97560e9563351a3797da3c56056ec881c23d46e42`。

隔离 Import 实测：source timeline 可查询；import status=accepted；runtime_ownership=dormant；空目标不物化 Project。

## 2. M1–M5 完成情况

| 里程碑 | 状态 | 冻结/完成提交 |
|---|---|---|
| M0 | 冻结基线 | `2ff20b2` |
| M1 | 完成 | `a9b44a1` |
| M2 | 完成 | `0a8e1ee` |
| M3 | 完成 | `7f93080` |
| M4 | 完成 | `114a622` |
| M5 | 完成 | 本报告对应冻结提交 |

## 3. Migration

Project Store：v1 → v2 → v3 → v4 → v5 → v6。从任一已发布 version 升级保持 ID、revision、event sequence、receipt 与 outbox；重复打开幂等。Portable schema version 为 `portable-1.0.0`，独立于 Protocol `1.0.0`。

## 4. 已知限制与剩余风险

- 空目标 CommitImport 不物化可查询 Project，只接受 dormant report。
- 导入后不会自动获得 Runtime ownership。
- Close/Archive/Cancel/Supersede 不改写 Change lifecycle_state。
- Windows 全量并行时 CLI 冷启动慢；M1/M2 CLI 超时为 240s，断言未降低。
- after-effect 故障注入依赖 2s 超时窗口，极端机器负载下仍可能竞争。
- 未发布全局安装包；云 DevOps、登录、实时协作、完整 SPA 未实现。

## 5. 给审核 Agent 的重点

1. Kernel 是否仍是唯一状态权威；CLI/Workbench/Adapter 是否只发 Command。
2. Human Decision 是否只记录事实后重新 Gate，而不是直接改 lifecycle。
3. 未解决 Refutes 与未完成 Mandatory Knowledge 是否阻止 ALLOW。
4. 测试/生产是否强制同一 Artifact digest。
5. 未知外部结果是否禁止盲重试。
6. Export 是否不含 secret；Import 是否拒绝 DIVERGENT_HISTORY 且 ownership=dormant。
7. Attention acknowledge 是否不会关闭 Decision Request。
